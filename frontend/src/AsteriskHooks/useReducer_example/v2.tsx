// The reduce() operation lets you take an array and “accumulate”(tích luỹ) a single value out of many:
const arr = [1, 2, 3, 4, 5];
const sum = arr.reduce((result, number) => result + number);

// The function you pass to reduce is known as a “reducer”
